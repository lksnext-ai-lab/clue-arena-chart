image:
  repository: ghcr.io/__GITHUB_OWNER__/clue-arena
  tag: dev

app:
  publicUrl: http://clue-arena.local
  auth:
    disableAuth: true
    authSecret: ci-auth-secret
  mcp:
    authToken: ci-mcp-token
