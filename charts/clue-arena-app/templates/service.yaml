apiVersion: v1
kind: Service
metadata:
  name: {{ include "clue-arena-app.fullname" . }}
  labels:
    {{- include "clue-arena-app.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clue-arena-app.selectorLabels" . | nindent 4 }}
